/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['w-blur-test.s3.amazonaws.com'],
    domains: ['unique-humans.s3.amazonaws.com'],
  },
}

export default nextConfig;
